---
const { pathname } = Astro.url;

const navLinks = [
  { title: "Events", path: "/" },
  { title: "Mød andre", path: "/people" },
];
---

<header>
  <h1>{Astro.url.pathname}</h1>
  <nav>
    <ul>
      {
        navLinks.map((link) => {
          return (
            <li>
              <a href={link.path} class={pathname === link.path && "active"}>
                {link.title}
              </a>
            </li>
          );
        })
      }
    </ul>
  </nav>
</header>

<style>
  ul {
    display: flex;
  }

  .active {
    color: #2C2520;
  }
</style>
